import 'package:flutter/material.dart';

class AllAccountPage extends StatelessWidget {
  const AllAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('All Account Page');
  }
}